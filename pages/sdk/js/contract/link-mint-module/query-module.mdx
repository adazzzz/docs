---
title: Query Module
description: The Query Module provides access to the repository storing all the modules that are available for use, as well as tools to retrieve link and mint modules, and more information about module metadata.
---

# Query Module

There is a [GitHub Repository](https://github.com/Crossbell-Box/Crossbell-Contracts/blob/main/deployments/modules.json) storing all the modules that are available for use. You can also check the [Module Store](/docs/info/module-store) page to browse through the modules.

## `getLinkModules`

Returns a list of link modules.

```ts
getLinkModules(): Promise<MintOrLinkModule[]>
```

### Usage

```ts
const modules = await contract.getLinkModules()
```

## `getMintModules`

Returns a list of mint modules.

```ts
getMintModules(): Promise<MintOrLinkModule[]>
```

### Usage

```ts
const modules = await contract.getMintModules()
```

## `getLinkModule`

Returns a link module by its address.

```ts
getLinkModule(address: string): Promise<MintOrLinkModule | undefined>
```

### Usage

```ts
const module = await contract.getLinkModule('0x1234')
```

## `getMintModule`

Returns a mint module by its address.

```ts
getMintModule(address: string): Promise<MintOrLinkModule | undefined>
```

### Usage

```ts
const module = await contract.getMintModule('0x1234')
```

## Module Metadata

Please refer to the [Module Metadata](/docs/specs/metadata/module-metadata) page for more information.
